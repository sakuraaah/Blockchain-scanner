import type { MenuItem } from '../types';

export const MAIN_MENU_ITEMS: MenuItem[] = [
  { key: 'scan', label: 'Scan', path: '/scan', variant: 'primary' },
  { key: 'about', label: 'About', path: '/about', variant: 'default' },
];
